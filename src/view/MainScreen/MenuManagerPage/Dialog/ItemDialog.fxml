<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.collections.FXCollections?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>
<?import java.lang.String?>

<VBox fx:id="dialogRoot" xmlns="http://javafx.com/javafx/25" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="view.MainScreen.MenuManagerPage.Dialog.ItemDialogController"
      stylesheets="@ItemDialog.css"
      spacing="12" styleClass="dialog-root">

    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>

    <Label fx:id="titleLabel" style="-fx-font-size: 18px; -fx-font-weight: bold;" text="Thêm món mới" />

    <Separator />

    <VBox spacing="4">
        <Label fx:id="lblName" text="Tên món:" />
        <TextField fx:id="nameField" promptText="VD: Cà phê sữa" />
    </VBox>

    <VBox spacing="4">
        <Label fx:id="lblPrice" text="Giá (VNĐ):" />
        <TextField fx:id="priceField" promptText="VD: 25000" />
    </VBox>

    <VBox spacing="4">
        <Label fx:id="lblCategory" text="Danh mục:" />
        <ChoiceBox fx:id="categoryChoice" prefWidth="200">
            <items>
                <FXCollections fx:factory="observableArrayList">
                    <String fx:value="Drink" />
                    <String fx:value="Food" />
                </FXCollections>
            </items>
        </ChoiceBox>
    </VBox>

    <HBox spacing="12" alignment="CENTER_LEFT">
        <ImageView fx:id="imagePreview" fitWidth="100" fitHeight="100" preserveRatio="true" style="-fx-border-color: #E2E8F0; -fx-border-width: 1; -fx-background-color: #F8FAFC;" />
        <Button fx:id="btnUpload" onAction="#onUploadImage" text="Tải ảnh" styleClass="primary-btn"/>
    </HBox>

    <Region VBox.vgrow="ALWAYS" />

    <HBox spacing="10" alignment="CENTER_RIGHT">
        <Button fx:id="btnCancel" onAction="#onCancel" text="Hủy" styleClass="dialog-button cancel"/>
        <Button fx:id="btnConfirm" onAction="#onConfirm" text="Lưu" styleClass="dialog-button confirm"/>
    </HBox>

</VBox>